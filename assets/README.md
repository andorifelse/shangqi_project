# External assets

Place real 3DGS `.ply` and vehicle `.glb` / `.gltf` files here. Paths in scene YAML are relative to the YAML file, never the working directory. Asset lengths are meters after explicit coordinate scale. Test assets are in `avm_sim_ws/src/avm_bringup/example_assets/`.

